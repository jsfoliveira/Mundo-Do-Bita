$(document).ready(() => {
  const slicOptions = {
    autoplay: true,
    autoplay: 2000, //velocidade para passar o slider
    dots: false, //pontinho embaixo que diz em que slider está
  };
  $(".slider").slick(slicOptions);
});
